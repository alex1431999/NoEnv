// In a real application, this secret should be stored in a secure way,
// for example, in a configuration service or as an environment variable.
export const jwtConstants = {
  secret: 'secretKey',
};
